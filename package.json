{
  "name": "lea-prototype-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel --filter \"./apps/*\" dev",
    "build": "pnpm -r --filter \"./apps/*\" build",
    "start": "pnpm -r --filter \"./apps/*\" start",
    "lint": "pnpm -r --filter \"./apps/*\" lint"
  }
}
